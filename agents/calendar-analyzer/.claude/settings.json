{
  "permissions": {
    "allow": [
      "Read",
      "Glob",
      "Grep",
      "Write(../../personal/meeting-notes/**)",
      "mcp__*"
    ],
    "deny": [
      "Bash(rm *)",
      "Bash(mv *)",
      "Bash(cp *)"
    ]
  },
  "_comment": "calendar-analyzer: read-only workspace access + all MCP tools (Google Calendar, Google Drive). Filesystem sandbox stays active — no access outside project."
}
